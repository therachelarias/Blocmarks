<h1> <%= @user.email %>'s Bookmarks </h1>

  <div class="row">
    <% @user_bookmarks.each do |bookmark| %>
    <div class="col-sm-3">
      <div class="well bookmark_box">
        <%= bookmark.url %> <br>
        <%= link_to "Delete", bookmark, method: :delete, class: 'glyphicon glyphicon-remove', data: { confirm: "Are you sure you want to delete this bookmark?" } %> <br>
        <%= link_to "Edit", edit_bookmark_path(bookmark), class: 'glyphicon glyphicon-pencil' %>
      </div>
      <div class="button_placement">
      <%= render partial: 'likes/like', locals: { bookmark: bookmark } %>
      </div>
    </div>
    <% end %>
  </div>
______________________

<h2> Liked Bookmarks </h2>

<div class="row">
  <% @liked_bookmarks.each do |like| %>
  <div class="col-sm-3">
    <div class="well bookmark_box">
      <%= like.bookmark.url %> <br>
      <%= link_to "Delete", like.bookmark, method: :delete, class: 'glyphicon glyphicon-remove', data: { confirm: "Are you sure you want to delete this bookmark?" } %>
      <%= link_to "Edit", edit_bookmark_path(like.bookmark), class: 'glyphicon glyphicon-pencil' %>
    </div>
    <div class="button_placement">
    <%= render partial: 'likes/like', locals: { bookmark: like.bookmark } %>
    </div>
  </div>
  <% end %>
</div>